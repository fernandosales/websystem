{% extends 'OroUIBundle:actions:update.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}

{% form_theme form with 'OroFormBundle:Form:fields.html.twig' %}

{% oro_title_set({params : {"%entityName%": entity.name }}) %}

{% if form.vars.value.id %}
    {% set formAction = path('fnz.institution.institution_update', { 'id': form.vars.value.id }) %}
{% else %}
    {% set formAction = path('fnz.institution.institution_create') %}
{% endif %}

{% block navButtons %}
    {% if form.vars.value.id and is_granted('DELETE', form.vars.value) %}
        {{ UI.deleteButton({
            'dataUrl': path('institution_api_delete_institution', {'id': form.vars.value.id}),
            'dataRedirect': path('fnz.institution.institution_index'),
            'aCss': 'no-hash remove-button',
            'id': 'btn-remove-tag',
            'dataId': form.vars.value.id,
            'entity_label': 'institution.entity_label'|trans
        }) }}
        {{ UI.buttonSeparator() }}
    {% endif %}
    {{ UI.cancelButton(path('fnz.institution.institution_index')) }}
    {% set html = UI.saveAndCloseButton() %}
    {% if is_granted('fnz.institution.institution_update') %}
        {% set html = html ~ UI.saveAndStayButton() %}
    {% endif %}
    {{ UI.dropdownSaveButton({ 'html': html }) }}
{% endblock navButtons %}

{% block pageHeader %}
    {% if form.vars.value.id %}
        {% set breadcrumbs = {
            'entity':      form.vars.value,
            'indexPath':   path('fnz.institution.institution_index'),
            'indexLabel': 'institution.entity_label'|trans,
            'entityTitle': form.vars.value.name
        } %}
        {{ parent() }}
    {% else %}
        {% set title = 'oro.ui.create_entity'|trans({'%entityName%': 'institution.entity_label'|trans}) %}
        {% include 'OroUIBundle::page_title_block.html.twig' with { title: title } %}
    {% endif %}
{% endblock pageHeader %}

{% block content_data %}
    {% set id = 'institution-edit' %}

    {% set dataBlocks = [{
            'title': 'institution.block.general'|trans,
            'class': 'active',
            'subblocks': [{
                'title': '',
                'data': [
                    form_row(form.name),
                    form_row(form.branchNumber),
                    form_row(form.iban),
                    form_row(form.bic),
                    form_row(form.address.country),
                    form_row(form.address.street),
                    form_row(form.address.city),
                    form_row(form.address.region),
                    form_row(form.address.region_text),
                    form_row(form.address.postalCode),
                ]
            }]
        }]
    %}
    {% set data = {
        'formErrors': form_errors(form)? form_errors(form) : null,
        'dataBlocks': dataBlocks,
    } %}
    {{ parent() }}
{% endblock content_data %}
