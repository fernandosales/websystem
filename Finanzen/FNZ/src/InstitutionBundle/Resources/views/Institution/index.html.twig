{% extends 'OroUIBundle:actions:index.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}
{% set pageTitle = 'institution.entity_label'|trans %}

{% block navButtons %}
    {% if is_granted('institution.institution_create') %}
        <div class="btn-group">
            {{ UI.addButton({
            'path': path('institution.institution_create'),
            'entity_label': 'institution.entity_label'|trans
            }) }}
        </div>
    {% endif %}
{% endblock %}
